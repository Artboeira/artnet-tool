// TypeScript types for all Tauri event payloads (Rust → Frontend)
// Event names use kebab-case: {subsystem}-{event}
// e.g., playback-state-changed, monitor-update, capture-started, error-occurred
// Populated as events are defined in each subsystem story.
export {};

// commands/keyboard.rs — Thin Tauri command wrappers for keyboard shortcuts
// Uses tauri-plugin-global-shortcut (OS-level, works when window is minimized)
// Populated in Story 4.1 (Keyboard Shortcut Assignment & OS-Level Scene Trigger)
// Commands: register_shortcut, unregister_shortcut

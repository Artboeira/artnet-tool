// commands/system.rs — Thin Tauri command wrappers for system/OS subsystems
// Populated across Epic 6 (auto-start, crash recovery) and Story 7.x (logging, updates)
// Commands: enable_autostart, disable_autostart, get_log, check_update
